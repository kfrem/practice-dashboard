Options -Indexes

RewriteEngine On

# Clean URL: /firmready → subscribe.php (URL stays as /firmready in browser)
RewriteRule ^firmready/?$ subscribe.php [L]

# Clean URL: /start → subscribe.php (URL stays as /start in browser)
RewriteRule ^start/?$ subscribe.php [L]
